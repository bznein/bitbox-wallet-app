[package]
name = "bitbox-ffi-qt"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
bitbox-bridge = { path = "../bitbox-bridge" }

[lib]
crate-type = ["cdylib", "rlib"]
path = "src/lib.rs"
